const express = require("express");
const router = express.Router();

// Example placeholder route (optional)
router.get("/", (req, res) => {
  res.send("Tesk route working");
});

module.exports = router;
